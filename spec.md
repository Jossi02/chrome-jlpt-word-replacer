# spec — 일본어 웹 콘텐츠 깊이 읽기 확장

> **상태: 설계 진행 중 / 미구현.** 코드는 한 줄도 없다.
> 최종 갱신: 2026-08-18 (2차 세션). 확정된 결정은 2~7절, **아직 정하지 않은 것은 9절**이다.
> 내일 이어서 할 때는 **9절부터** 보면 된다. 각 항목에 문제와 추천안이 함께 적혀 있다.
>
> 설계 논쟁의 전체 이력(철회된 판단, 폐기된 결정, 반론 42건)은 별도 기록에 있다:
> `C:\Users\hersm\.claude\plans\spec-md-whimsical-noodle.md`
> 이 문서는 **결론만** 담는다. 이유가 궁금하면 그 기록을 본다.

---

## 1. 이 제품이 하는 일 (한 문단)

일본어로 된 웹 콘텐츠(웹소설, 커뮤니티 글·댓글)를 읽을 때, **사용자가 아는 단어가 얼마나 되는지에
따라 문장 단위로 원문과 번역을 섞어 보여준다.** 처음에는 거의 전부 한국어로 보이고, 사용자가
"이 단어 안다"를 체크해 나가면 문장들이 점점 일본어 원문으로 바뀐다. 즉 **좋아하는 콘텐츠 자체가
단어장이 된다.** 동시에, 번역하면 죽는 표현(반말/존댓말 전환, 「ツンデレ」 같은 말)에는 밑줄이
그어져 있어, 번역문을 읽는 동안에도 무엇을 잃고 있는지 알 수 있다.

---

## 2. 목적

읽고 싶은 콘텐츠를 이해하는 **고통을 낮추면서**, 동시에 **번역기가 온전히 전달하지 못하는
감정·뉘앙스를 이해하고 싶은 욕망**도 채운다.

해결하려는 좌절 구간은 둘이다.

1. 모르는 단어를 마주칠 때마다 멈춰 사전을 찾으면 **콘텐츠 몰입이 깨진다**
2. 언제 쓸지 모르는 단어를 **단어장으로 달달 외워야 한다**

이 둘이 언어 학습 욕망을 가진 사람을 좌절시키는 가장 큰 관문이고, 제품의 목적은 그 구간을
완화하는 것이다.

**정직한 포지셔닝**: 이것은 "언어 학습 앱"이 아니라 **"번역기가 지우는 것을 되살리는 깊이 읽기
도구"**다. 학습은 약속이 아니라 부작용으로 말해야 한다. (이유는 10절 참조)

**참고 — 이 접근은 새롭지 않다.** 특정 목적을 위한 언어 학습(ESP) + 다독 + 글로싱은 응용언어학의
정통 접근이다. 차별점은 교육학이 아니라 **배달 방식** — 사용자가 이미 읽고 있는 실제 콘텐츠 안에서,
실시간으로 일어난다는 것.

---

## 3. 핵심 메커니즘 — 밀도에 따른 3단 척도

### 3.1 상태는 단 하나다

저장하는 것은 **사용자 어휘 체크 목록 하나**뿐이다. 화면에 무엇이 어떻게 보이는지는 그 목록에서
**매번 결정론적으로 다시 계산**된다.

이것은 SRS(간격 반복 학습)가 **아니다.** 복습 주기도, 간격도, 점수도, 문장별 저장 상태도 없다.
어휘 목록 하나를 렌즈로 삼아 콘텐츠를 다시 그리는 것이다.

### 3.2 표시 규칙

사용자가 감당할 수 있는 모르는 단어의 밀도 `k`를 슬라이더로 정한다(예: 20단어당 1개 = 95% 커버리지).
한 문장의 모르는 **내용어** 수를 `u`, 그 문장에 허용된 예산을 `k'`라 하면:

| `u` | 표시 | 예 — 「彼は剣を抜いた」(그는 검을 뽑았다) |
|---|---|---|
| `u = 0` | 통째 **원문** | 彼は剣を抜いた |
| `0 < u ≤ k'` | 원문 + **그 `u`개 단어만 한국어로 치환** | 彼は剣を **뽑았다** |
| `u > k'` | 통째 **번역** | 그는 검을 뽑았다 |

- `k'` = `k × 문장 길이 / n`. **하한은 1** — 짧은 문장에서 2행이 영구히 비지 않도록
- 사용자에게 노출하는 손잡이는 **`k` 하나뿐**이다. 두 경계 모두 `k`에서 파생된다
- 슬라이더를 움직이면 **지금 보고 있는 페이지의 표시가 실시간으로 바뀐다.** 사용자는 자기 수준을
  숫자로 몰라도 "이 정도면 읽을 만하네"는 3초 안에 판단한다. 자기신고를 쓰지 않는 이유

### 3.3 승격

어휘가 늘면 각 문장이 **아래 → 위로 자동 승격**한다. 처음에는 거의 모든 문장이 3행(통째 번역)이고,
체크가 쌓이면 2행(원문 + 일부 치환)을 거쳐 1행(통째 원문)이 된다.

**이 승격 비율이 이 제품의 진행 지표다.** "원문으로 표기되는 문장의 비율"은 관측 가능하고
사용자에게 그대로 보여줄 수 있다.

### 3.4 체크와 조회

- 원문으로 표시된 단어를 눌러 **"안다"를 체크**한다
- 체크는 **읽는 중 언제든** 할 수 있다
- 문장을 눌러 **원문 ↔ 번역을 토글**할 수 있다.
  → 이 토글은 "이 문장 못 읽었다"의 정확한 신호이므로, **잘못된 체크를 되돌리는 장치로 쓸 수 있다**
- 화 끝에는 "이 화에 나온 원문 단어" **일괄 체크 목록**도 제공한다

### 3.5 화면은 즉시 바뀌지 않는다

체크는 즉시 저장되지만 **화면은 그 자리에서 다시 그리지 않는다.** 그러면 위쪽 문단 길이가 바뀌어
지금 읽던 줄이 움직이고, 그건 몰입 파괴 중 가장 나쁜 종류다.

- 재렌더 시점: **URL이 바뀔 때**(페이지 이동). 사이트 구조를 몰라도 항상 성립한다
- 무한 스크롤: 이미 그려진 부분은 손대지 않고 **새로 로드되는 부분에만** 새 어휘 목록을 적용한다
- "지금 적용" 버튼으로 사용자가 명시적으로 재렌더할 수도 있다

**그 대신 즉시 보이는 것이 반드시 있어야 한다** — 누른 순간 그 단어에 표시가 남고,
화면 어딘가에 **"이번 화에서 체크 12개 · 다음 화부터 37문장이 원문으로 바뀝니다"**가 실시간으로
올라간다. 이건 장식이 아니라 필수 부품이다. 체크의 보상이 지연되므로 예고가 없으면 왜 누르는지
모르게 된다.

---

## 4. 대상 언어쌍 — 일본어 → 한국어

**취향이 아니라 3.2의 2행(단어 치환)이 성립하는 조건이다.**

일본어와 한국어는 어순(주어-목적어-동사)과 조사 구조가 거의 같아서
「彼は剣を **뽑았다**」가 실제로 읽힌다. 영어 → 한국어에서는 어순이 달라 이게 깨지고, 2행이
사라지면 3단 척도가 2단으로 퇴화해 이 설계의 핵심이 없어진다.

### 4.1 한자어 보너스 — "사용자는 아무 단어도 모른다"는 가정은 일→한에서 거짓이다

한국인은 **한자어를 이미 안다.** 「剣」「時間」「学校」「安全」은 배우지 않고도 뜻이 추론된다.
따라서 시작 시점의 실제 상태는 "0% 이해"가 아니라 **"한자 명사의 상당 비율을 이미 읽는 상태"**다.

이것이 세 가지를 동시에 해결한다.

- **첫날부터 체크할 것이 있다.** 승격이 즉시 시작되므로 "쓸모없는 번역기" 구간을 건너뛴다
- **부트스트랩 규칙이 여기서 나온다**: 처음에 원문으로 노출할 문장을 무작위로 뽑지 않고,
  **한자어 비중이 높은 짧은 문장**을 먼저 노출한다. 정적 데이터만으로 계산되고 AI가 필요 없다
- **경쟁 우위다.** 이 보너스는 한국어·중국어 사용자에게만 존재한다. 영어권 사용자용으로 설계된
  Yomitan·Migaku는 이걸 활용할 수 없다

### 4.2 그 보너스의 청구서 — 거짓짝

「勉強」은 면강이 아니라 **공부**, 「大丈夫」는 대장부가 아니라 **괜찮다**, 「怪我」는 **부상**,
「切手」는 **우표**다. 훈독어·화어(和語)는 애초에 겹치지 않는다.

**위험의 성질이 나쁘다**: 사용자는 "안다"고 확신하고 체크하며, 틀렸다는 신호를 받지 못한다.
일본어→한국어를 고른 대가로 **정확히 이 오류만 빈발한다.**

→ 거짓짝은 유한하고 목록화 가능하다(수백~수천 개 규모). **정적 블랙리스트를 한자 대응표와
동시에 만들어야 한다.** 나중에 붙일 수 없다.

### 4.3 필요한 언어 자산

| 자산 | 용도 | 출처 |
|---|---|---|
| 형태소 분석기 (MeCab / Sudachi) | 일본어는 띄어쓰기가 없어 단어 경계를 나눠야 한다. 내용어/기능어 구분에도 필요 | 무료·성숙 |
| 빈도 코퍼스 | 어느 단어를 원문으로 남길지, 어느 문장을 먼저 노출할지 | 무료·정적 |
| 한자어 대응표 | 한자어 보너스 활용 | 한자음 표 + 음독 데이터로 생성 |
| 거짓짝 블랙리스트 | 4.2 | 직접 제작 |
| 후리가나(읽기) 사전 | 발음 병기 | 무료·정적 |
| 뉘앙스 사전 | 6절의 B | 직접 제작 (9절 미결) |

---

## 5. 아키텍처 — 서버 0 · 실시간 AI 호출 0 · 원가 0

### 5.1 왜 이게 가능한가

| 원래 필요했던 것 | 어디로 갔나 |
|---|---|
| 사용자별·문장별 상태 저장 | **불필요.** 표시는 어휘 목록 하나에서 매번 재계산된다 |
| 원문을 서버에 저장 | **불필요.** 저장할 상태가 없으므로 원문을 보낼 이유도 없다 |
| 대량 기계번역 서버 | **브라우저 내장 기능으로 대체** (5.2) |
| 뉘앙스 설명 실시간 AI 호출 | **미리 만든 정적 사전 조회로 대체** (6절) |

**결과: 서버 0, 실시간 AI 호출 0, 사용량 비례 원가 0, 원문 전송 0, 추적 0.**

### 5.2 번역 — Chrome 내장 Translator API (온디바이스)

Chrome 138부터 브라우저에 온디바이스 번역이 내장돼 있다(Gemini Nano 기반).

- API 키 없음, 요청당 비용 없음, **텍스트가 기기를 떠나지 않음**, 오프라인 동작
- 언어 모델은 언어팩으로 필요 시 다운로드된다
- **지원 여부는 정적 목록으로 판단하지 말고 런타임에 조회한다:**
  ```js
  const ok = await Translator.availability({sourceLanguage: 'ja', targetLanguage: 'ko'});
  // 'unavailable'이 아니면 사용 가능 ('available' | 'downloadable' | 'downloading')
  const translator = await Translator.create({sourceLanguage: 'ja', targetLanguage: 'ko'});
  // 미지원 조합이면 NotSupportedError
  ```
- 프라이버시 보호를 위해 언어팩별 다운로드 상태를 숨기므로, 모든 조합이 처음에는
  `downloadable`로 보고된다
- 참고 구현: `lete114/chrome-translate` — 본문 텍스트 노드 추출 → 원문+번역 인라인 표시, LFU 캐시

**번역 호출부는 인터페이스 하나로 감싼다.** 나중에 LLM 번역 경로를 붙이는 것이 구조 변경이 아니라
덧붙이기가 되도록.

### 5.3 플랫폼 제약 (5.2의 대가)

- **Chrome MV3 · 데스크톱 전용.** 모바일·Safari·Firefox 불가
- 첫 실행 시 언어팩 다운로드 (온보딩 마찰)
- 저장 공간 여유 요건이 있고, 부족해지면 모델이 삭제된다

### 5.4 어휘 목록의 보관 — 사용자의 유일한 자산

사용자가 몇 달에 걸쳐 쌓는 것은 어휘 체크 목록 하나다. 그게 진행 감각이고 재방문 이유다.
서버가 없으므로 **브라우저 프로필을 바꾸면 전부 사라진다.** 그건 제품 폐기와 같다.

서버 0을 유지하면서 푸는 방법:

- **`chrome.storage.sync`** — Chrome 계정으로 기기 간 동기화된다. **우리 서버가 아니다.**
  용량 상한이 작으므로(전체 100KB 규모 — 정확한 수치는 구현 시 확인) 표제어 문자열이 아니라
  **사전 인덱스 ID만** 저장하는 압축 표현이 필요하다. 그러면 수천~수만 단어가 들어간다
- **JSON 내보내기 / 가져오기 버튼** — 서버 0 제품에서 이건 예의가 아니라 필수 기능이다
- 부수 효과: ID 배열 압축은 3.1의 재계산 속도에도 유리하다

### 5.5 페이지를 다시 쓴다는 것의 실무 비용

사이트별 어댑터 없이 범용으로 동작하려면 다음을 스스로 해결해야 한다. **구현 난이도의 실제
대부분이 여기 있다.**

- **본문이 어디인지 사이트 지식 없이 찾기** — 광고·댓글·내비게이션·UI 문자열을 번역하면 페이지가
  망가진다. Readability 계열 본문 추출이 필요하고 실패율이 0이 아니다
- **사이트가 자기 텍스트를 다시 쓰면 우리 변경이 지워진다** — SPA·무한 스크롤·프레임워크 리렌더.
  MutationObserver로 따라가되, 우리 변경과 사이트 변경을 구분해 무한 루프를 피해야 한다
- **문장 경계와 DOM 경계를 맞추기** — 한 문장이 `<span>`·`<ruby>`·`<em>`으로 쪼개져 있으면
  3.2의 문장 단위 표시가 성립하지 않는다. **일본어 웹소설 사이트는 루비 태그를 특히 많이 쓴다**
- 위험이면서 기회: **사이트가 이미 달아둔 루비를 읽어서 발음 데이터로 쓸 수 있다**

### 5.6 저작권

서버에 아무것도 저장하지 않으므로 저장 위험은 없다. 번역 자체는 2차적 저작물 작성이지만,
실무 위험은 낮다 — Google 번역·Immersive Translate가 이미 대규모로 같은 일을 한다.
유료 회원제 사이트에서 동작할 때의 논란은 별도로 생각해야 한다.

---

## 6. 두 개입의 분리 — 그리고 색 규칙

### 6.1 A와 B는 반대 방향으로 작동한다

원래 "일부 번역"이라는 하나의 말에 뭉개져 있던 두 가지를 갈랐다.

| | **A. 이해 바닥 받치기** | **B. 뉘앙스 복원** |
|---|---|---|
| 막는 실패 | 포기(의지 상실) | 무지(무엇을 잃었는지 모름) |
| 판단 기준 | 사용자 대비 **단어 희귀도** | **번역 손실도** — 사용자와 무관, 텍스트 내재 속성 |
| 개입 내용 | 짧은 뜻 | 결에 대한 설명 |
| 방향 | 마찰 **제거** (쉽게 읽히게) | 마찰 **추가** (멈춰서 보게) |
| 원가 | 로컬 사전 + 빈도표. 서버 0 | 정적 사전 조회. 서버 0 |
| 표시 | **조용한 회색 / 무표시** | **색 있는 밑줄** |
| 차별성 | 없음 — Yomitan이 이미 무료로 함 | **제품의 정체성** |

3절의 척도가 A이고, B는 그 위에 얹힌다.

### 6.2 색 규칙 — 구현 전에 못 박아야 하는 것

3.2의 3행(통째 번역) 안에 원문 단어를 남기면(뉘앙스 보존), 화면상 그것은 2행(원문 + 일부 번역)과
**정확히 거울상**이라 구분되지 않는다. 사용자는 「ツンデレ」가 원문인 이유를 알 수 없다 —
내가 안다고 체크해서인가, 번역이 불가능해서인가. 두 개입의 의미가 뒤섞이면 둘 다 망가진다.

> **원문으로 보이는 단어에 색이 있으면 "번역하면 죽는 것", 색이 없으면 "네가 아는 것".**

이 규칙이 지켜지면 두 개입이 같은 화면에 공존하고, 시작 시점(거의 전부 3행)에도 B가 가치를 만든다.

### 6.3 B는 AI를 실시간으로 부르지 않는다

번역된 문장 안의 고손실 표현에 **색 있는 밑줄만** 긋고, 누르면 사전 항목(원문 표기 + 결에 대한
짧은 설명)이 뜬다. **설명은 생성되지 않고 조회된다.**

사전은 AI로 초안을 일괄 생성하고 사람이 검수해 만든 뒤 정적 파일로 배포한다.
즉 **AI가 제품에서 빠진 것이 아니라 런타임에서 빌드 타임으로 이동한 것**이다.

이 선택으로 동시에 사라지는 위험:

- **환각** — 런타임 생성이 없으므로 환각할 지점이 없다
- **검증 불가능성** — 사전 항목은 사람이 검수한 고정 텍스트다. 틀리면 고칠 수 있고, 고치면 전역으로
  고쳐진다. 매번 새로 생성되는 설명과 신뢰 성질이 근본적으로 다르다
- **원가** — 사용량에 비례하는 비용이 없다
- **프라이버시** — 서버로 나가는 것이 없으므로 질문 자체가 사라진다
- **"무엇을 잃었는지 모른다" 문제** — 사용자가 눈치채지 못한 손실을 제품이 먼저 표시한다.
  이게 온디맨드 조회로는 원리적으로 불가능했던 것이다

대신 새로 생기는 위험은 **사전이 늙는다**는 것이다 (10절).

### 6.4 번역 품질과 뉘앙스의 역할 분담

- **기계번역의 역할 = 이해의 발판.** 단어 뜻을 확정할 만큼의 문맥을 주면 임무 완료
- **뉘앙스의 역할 = B의 밑줄.** 목적의 두 번째 절반은 번역이 아니라 B가 담당한다

두 역할이 분리되어 있으므로 **기계번역 품질이 이 제품의 목적을 직접 위협하지 않는다.**
품질이 다소 떨어지는 번역도 단어 학습에 필요한 문맥은 충분히 제공한다.

---

## 7. MVP 범위

### 들어가는 것

- 3절의 3단 척도 표시 엔진 (일본어 → 한국어)
- 형태소 분석 기반 단어 경계 + 내용어/기능어 구분
- 어휘 체크 UI + `chrome.storage.sync` 동기화 + JSON 내보내기/가져오기
- 밀도 슬라이더 (실시간 미리보기)
- 문장 원문↔번역 토글
- 한자어 비중 기반 부트스트랩 노출
- 승격 예고 표시 ("다음 화부터 N문장이 원문으로")
- Chrome 내장 Translator API 연동 (인터페이스로 감싸서)
- B의 밑줄 — 범위는 9절 미결

### 안 들어가는 것

- **유료 LLM 티어** — 방향만 기록. 무료·오픈소스 + LLM 유료 티어(더 좋은 번역과 해석)를 나중에
  붙인다. 온디바이스 + 서버 0으로 만들어두면 이건 덧붙이기이지 구조 변경이 아니다
- **수익 모델 전반** — MVP 이후. 원가가 0이므로 지금 결정을 강제하는 것이 없다
- **두 번째 언어쌍** — 언어별 자산을 전부 다시 만들어야 한다
- 모바일, Safari, Firefox
- PDF

---

## 8. 코딩 전에 반드시 재봐야 하는 것 (최우선)

**Chrome 내장 Translator API의 일본어 → 한국어 품질 실측.**

Google의 온디바이스 번역 스택은 비영어↔비영어 쌍에서 **영어를 중간 언어로 경유**하는 것으로
알려져 있다. 사실이라면 일→한이 **일→영→한**이 되어 번역 손실이 두 번 발생한다.
그리고 시작 시점 화면은 거의 전부가 3행(통째 번역)이므로, 신규 사용자의 첫 경험 전체가 그 번역이다.

**방법**: 일본 웹소설 문장 50~100개를 골라 나란히 비교한다.

1. Chrome 내장 Translator API
2. DeepL 등 일→한 직접 번역
3. 사람 번역 (공식 번역본이 있으면 그것)

**결과가 아키텍처를 바꾼다.** 품질이 부족하면 BYO API 키 또는 자체 번역 서버로 후퇴해야 하고,
그 순간 서버 0이 깨진다. 6.4의 역할 분담 때문에 결과가 나빠도 설계 전체를 뒤집지는 않지만,
무료/유료 경계를 어디에 그을지가 여기서 결정된다.

---

## 9. 아직 정하지 않은 것 (내일 이어갈 지점)

각 항목은 **문제 → 추천안** 형식이다. 추천안이 이상해 보이는 것만 골라서 토의하면 된다.

### 9.1 B(밑줄)를 MVP에 어디까지 넣나 — 가장 큰 미결

**문제**: 6.3의 "미리 만들어둔 설명"을 누군가 써야 한다. 「ツンデレ」가 왜 번역하면 죽는지,
「僕」과 「俺」이 어떻게 다른지가 어딘가에 미리 적혀 있어야 밑줄을 눌렀을 때 나온다.
**즉 MVP 작업의 상당 부분이 코딩이 아니라 글쓰기가 될 수 있다.** 이게 이 제품의 유일한 실질
비용이고, 동시에 경쟁자가 복제하기 어려운 유일한 자산이다.

**추천안**: **경어체↔반말 전환 + 1인칭 대명사만 먼저.** 이 둘은 사람이 만드는 목록이 거의 0이다 —
문장 끝 형태(です·ます 대 だ·だよ)를 보면 프로그램이 자동으로 잡고, 1인칭은 단어가
僕·俺·私·あたし·わたくし 정도로 대여섯 개뿐이다. 사람이 쓰는 것은 유형당 설명문 한 번씩.
그런데도 일본어에서 번역 손실이 가장 큰 항목이다. 의성어·의태어(수백~수천 항목)와
밈·커뮤니티 은어는 그다음 단계로 미룬다.

### 9.2 조사·조동사를 예산에서 빼나

**문제**: 3.2에서 모르는 단어 수를 셀 때 조사(は·が·を·の)와 조동사(です·た)를 포함하면,
초보자에게는 그것들이 전부 모르는 단어여서 **첫 문장에서 예산이 즉시 소진된다.**
일본어 학습에서 조사는 어휘가 아니라 문법으로 배운다.

**추천안**: 기능어는 **예산 계산에서 제외하고 항상 원문으로 둔다.** 별도 범주로 만든다.
형태소 분석기가 품사를 주므로 추가 데이터가 필요 없다.

### 9.3 예산 안에서 "어느" 단어를 원문으로 남기나

**문제**: 3.2는 "몇 개"를 정하지만 "어느 것"을 정하지 않는다. 20단어당 2개를 허용한다면 어느
2개인가? 핵심 명사·동사를 고르면 독자가 길을 잃고, 사소한 걸 고르면 배울 게 없다.

**추천안**: **코퍼스 빈도 내림차순.** 흔한 단어일수록 (a) 예측 가능한 문맥에 나타나 추론이 쉽고,
(b) 앞으로 계속 만나므로 배울 값어치가 크다. 보통 충돌하는 두 기준이 빈도 상위에서 일치한다.
정적 빈도표만으로 계산된다.

### 9.4 단어 조회를 어떤 동작으로 하나

**문제**: "특정 모드에 들어가서 더블클릭"으로 하면, 막힐 때마다 모드 진입 → 조회 → 이탈을
반복하게 된다. 그게 원래 없애려던 그 몰입 파괴다. 모드가 필요한 이유는 더블클릭이 브라우저 기본
텍스트 선택과 충돌하기 때문일 것이다.

**추천안**: 모드 대신 **제스처** — 호버 + 짧은 지연, 또는 Alt/Shift를 누른 상태로 클릭.
다만 "읽기 모드 / 조회 모드" 분리도 방어 가능하다. 읽는 중 오작동을 막고 흐름을 보호한다.
의도적 분리라면 대가는 사용자가 지금 어느 모드인지 기억해야 한다는 것뿐이다.

### 9.5 체크의 단위 — 표제어냐 활용형이냐

**문제**: 「抜いた」를 체크하면 「抜く」「抜かない」「抜ける」도 아는 것인가?
표제어(원형)로 저장하는 것이 유일하게 확장 가능한 답이지만, **초보자에게는 「抜いた」와 「抜く」가
같은 단어라는 사실 자체가 학습 대상**이다. 표제어를 알아도 활용을 모르면 그 문장을 못 읽는다.
그리고 「食べさせられた」를 몇 조각으로 볼 것인가 — 형태소 분석기의 최소 단위는 초보자에게 의미
단위가 아니다(Sudachi의 분할 모드 선택이 UX 결정이 된다).

**추천안**: 표제어로 저장한다. 대신 처음 만나는 활용형은 **그 활용만 짧게 주석**한다.
활용형 목록은 유한하고 규칙적이라 정적 데이터로 충분하다.

### 9.6 발음(후리가나)을 항상 병기하나

**문제**: 한국인은 「剣」의 뜻이 "검"임을 안다. 그러나 「けん」이라고 읽는 법은 모른다.
체크 목록이 "뜻을 안다"만 기록하면 사용자는 일본어를 **한국식 한자로 훑는** 습관이 굳는다.
원문 비율 지표는 빠르게 오르는데 일본어 발음은 하나도 늘지 않는다.
읽기 도구로는 결함이 아니지만, "언어 학습"으로 팔면 배신감이 된다.

**추천안**: **후리가나를 항상 병기한다.** 일본어에서 문화적으로 완전히 자연스럽고 침습도가 가장
낮은 주석 형식이며, 원문을 전혀 가리지 않는다. 비용 0(정적 사전). **읽으려 하지 않아도 눈에
들어오는 것이 루비의 미덕이다.** 단 5.5의 기존 루비 태그와 충돌하지 않게 처리해야 한다.

### 9.7 한자어 거짓짝 블랙리스트

**문제**: 4.2. 사용자가 확신하고 틀리게 체크하는데 틀렸다는 신호를 못 받는다.

**추천안**: 블랙리스트에 든 단어는 **자동 추론 대상에서 제외하고 항상 뜻을 병기한다.**
한자 대응표를 만들 때 같이 만든다.

### 9.8 잘못된 체크를 어떻게 감지하나

**문제**: 자기신고 어휘 지식은 실증적으로 과대평가된다. 그리고 **체크는 단어 형태에 붙는데 의미는
문맥에 붙는다** — 「かける」를 "걸다"로 알고 체크했지만 이 문장에서는 "(돈을) 걸다"다.
한 번 체크되면 전역으로 아는 것이 되어 못 읽는 문장들이 계속 승격된다.

**추천안**: **토글이 곧 강등 신호다.** 원문 문장을 눌러 번역을 여는 행동(3.4)은 "이 문장 못
읽었다"의 정확한 신호이고 이미 UI로 존재한다. 반복 토글되는 문장의 체크를 재확인 요청하면 된다.
**별도 채점 UI 없이 공짜로 얻는 피드백 루프.**

### 9.9 체크 UI를 읽는 중에 두나

**문제**: 원래 고통은 "모르는 단어마다 멈춰 사전 찾기"였는데, 새 고통은 "아는 단어마다 멈춰
체크하기"다. 결정적 차이는 **체크가 단어당 평생 1회**이고 사전 조회는 매번 반복된다는 것이므로
총량은 확실히 줄어든다. 그럼에도 초기 구간의 체크 밀도가 가장 높은데 보상은 가장 낮다.

**추천안**: 인라인 체크와 **읽은 뒤 일괄 체크를 병행**한다(3.4에 이미 있음).
읽는 중엔 눈에 걸리는 것만 누르고, 나머지는 화 끝 목록에서 한꺼번에.

### 9.10 본문 추출과 DOM 재작성

**문제**: 5.5. 구현 난이도의 실제 대부분.

**추천안**: MVP에서는 대상 사이트를 **한두 곳으로 좁혀 검증**한 뒤 범용화한다.
"사이트별 어댑터를 만들지 않는다"는 원칙은 유지하되, 테스트 대상은 좁힌다.

---

## 10. 아직 해소되지 않은 반론

여기 있는 것들은 답이 없거나, 답이 있어도 대가가 남는 것들이다.
(해소된 반론과 철회된 판단은 플랜 파일에 있다)

### 10.1 문장 구조 사각지대 — 단어 단위 모델의 원리적 한계

모르는 단어가 0개여도 못 읽는 문장이 많다. 긴 종속절, 일본어에서 생략된 주어, 이중부정.
3.2의 밀도 계산은 이걸 **전혀** 못 잡는다. "단어 수준 개입만으로 의지 상실을 막는다"는 가정은
틀렸다. 9.5의 활용형 문제도 이것의 특수형이다.
1차 버전에서 유보는 가능하지만, 해결됐다고 착각하면 안 된다.

### 10.2 사전이 늙는다

6.3에서 환각을 없앤 대신 **정반대 실패(침묵)**를 얻었다. 커뮤니티 은어와 밈은 유행 주기가 짧아,
사전이 고정되면 6개월 뒤 "요즘 말"을 하나도 모르는 도구가 된다.

→ 완화안: (a) 사전을 확장 코드에서 분리해 **정적 파일로 주기 갱신** — 서버 0을 깨지 않는다,
(b) 사용자가 만난 미등록 표현을 **신고하는 버튼** 하나. 표현 문자열만 보내면 되므로 원문 저장·
프라이버시 문제가 없고, **신고 로그가 다음 사전 판의 우선순위 목록이 된다.**
오픈소스는 이 문제를 커뮤니티 기여로 푸는 유일한 현실적 경로이고, Yomitan이 정확히 그 방식으로
일본어 학습 도구 시장을 가져갔다.

### 10.3 포지셔닝

추적·복습이 없고 발음도 안 가르치는(9.6 미결) 제품을 "언어 학습 앱"으로 내면, 학습을 기대한
사용자가 와서 이탈한다. 2절의 결론을 지켜야 한다 — 학습은 약속이 아니라 부작용으로 말한다.

더 정직한 약속의 형태: "일본어를 배운다"가 아니라 **"이 시리즈를 원문으로 읽게 된다"**.
웹소설은 내부 반복률이 비정상적으로 높으므로(같은 캐릭터·설정 용어·문체·작가 습관)
*그 시리즈에 대해서는* 실제로 매우 효율적이다. 좁지만 지킬 수 있는 약속이다.

### 10.4 경쟁 — 빈 시장이 아니라 붐비는 시장의 빈 사분면

- **Yomitan** — 일본어 호버 사전을 이미 무료로 제공한다. 즉 **6.1의 A는 이미 존재한다**
- **Toucan** — 이 아이디어의 거울상(모국어 페이지의 일부 단어를 목표어로 치환)
- 그 외 Language Reactor, Readlang, LingQ, Migaku

빈틈은 **B(뉘앙스·경어체·밈 설명)**와 **한국어 사용자용 한자어 활용(4.1)**이다.

**진짜 방어선은 AI 역량이 아니라 마찰 제거다.** 뉘앙스 설명 자체는 이미 AI에 물어보면 되지만,
사용자는 웹소설 읽다가 300문장을 복사해 붙이지 않는다. 이걸 인정해야 "그래서 뭐가 다른데"에
정직하게 답할 수 있다.

### 10.5 유료 티어를 붙이는 시점에 되살아나는 것들

7절에서 미룬 것들이 실제로 붙을 때 다시 꺼내야 한다.

- **원가 곡선이 지불 의사 곡선과 정반대다.** LLM 번역이 필요한 건 3행 문장뿐이고 어휘가 늘면
  줄어든다 → **초보자가 가장 비싸고 숙련자는 거의 무료다.** 유료 사용자의 마진은 시간이 갈수록
  개선되지만(좋다), 무료 체험은 최대 손실 구간을 정확히 공짜로 주는 것이다(나쁘다).
  완화: 번역 결과 **로컬 캐시**(문장 번역은 어휘와 무관하므로 그냥 성립), 체험을 기간이 아니라
  **번역 분량**으로 주기
- **무료/유료를 품질로 가르면 무료 티어가 첫인상을 망칠 수 있다.** 8절의 실측 결과에 달렸다.
  대안은 경계를 **양**으로 긋는 것 — 무료도 LLM 번역을 쓰되 분량 제한
- **오픈소스인데 유료 티어가 있다.** 막으려 하면 포크가 나온다.
  → 막지 말고 공식화한다: **"BYO 키는 공식 지원 기능이고, 유료 구독은 키 없이 쓰는 것이다."**
  뜻밖의 이득 — 가장 무거운 사용자가 자발적으로 BYO 키로 이동하므로 손실 구조가 자기 해소된다
- 유료 티어에서는 환각·검증 불가능성·프라이버시가 되살아난다. 무료 사용자는 여전히 서버 0이다

### 10.6 이론적 뒷받침 (기록용)

Nation: 읽기를 통한 우연적 어휘 습득에는 **알고 있는 단어 커버리지 95~98%**가 필요하다.
3.2의 밀도 슬라이더가 하는 일이 정확히 이 임계선을 사용자가 직접 정하게 하는 것이고,
`k` = 20단어당 1개가 95%에 해당한다. 이 설계는 그 연구와 정합한다.

단, 같은 표현을 8~20회 만나야 정착한다. 복습 장치가 없으므로 실제 습득 속도는 느리다.
이는 결함이 아니라 선택의 결과이며, 10.3의 포지셔닝으로 처리해야 한다.
