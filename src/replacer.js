// src/replacer.js — 텍스트 노드 치환 + 무손실 복원
window.KOJA = window.KOJA || {};
(function () {
  'use strict';

  function applyMatches(textNode, picks) {
    return 0;
  }

  function restoreAll(root) {
    return 0;
  }

  window.KOJA.replacer = {
    applyMatches: applyMatches,
    restoreAll: restoreAll
  };
})();
