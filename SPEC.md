# SPEC v2.2 — KoJa · 웹서핑 중 일본어 단어 노출 확장

- 최종 갱신: 2026-08-19
- 상태: **설계 확정 · 사전 646개(검증 통과) · D3 실측 반영 완료**
- 제약: **3인 · 달력 3일** (약 18~27 인시)

## v2.2에서 달라진 것 — D3-2 실측 오탐 제거

D3-1 실측(데모·위키백과·뉴스 3사이트, N1·밀도 100%)에서 확인한 오탐 4건을 반영해 **사전이 648개에서
646개로 줄었다.** 설계와 규칙은 바뀌지 않았고 데이터와 예외 하나만 바뀌었다.

| 항목 | v2.1 | v2.2 |
|---|---|---|
| 엔트리 | 648 (N5 328 / N1 50) | **646** (N5 327 / N1 49) |
| 매칭 표면형 | 680 (대표 648 + 별칭 32) | **677** (대표 646 + 별칭 31) |
| `ruby` 대상 / 가나 전용 | 544 / 104 | **542** / 104 |
| `dictionary.js` 크기 | 82.6 KB | **82.4 KB** |
| 부분 문자열 쌍 | 74 | 74 (변화 없음) |

- 삭제: 「사전」(辞書 — "사전 투표"의 事前과 충돌) · 「고려」(考慮 — 고려 왕조와 충돌)
- 별칭만 제거: 「상의」(上着 — "동해 상의"의 "상+의"와 충돌). 대표 표제어 「겉옷」은 유지
- 규칙 예외 1건: 「바람」은 사전에 남기고 `matcher.js`에서 관용구 `~는 바람에`만 제외

실측 기록은 `docs/misfires.md`, 안전성 회귀는 `docs/d3-3-safety.md`에 있다.
**아래 「v2.1에서 달라진 것」은 그 시점의 기록이므로 648/680이 그대로 남아 있다.**

## v2.1에서 달라진 것

v2.0(세 SPEC 통합본) 이후 사전이 확정되어, 열려 있던 데이터 항목이 전부 닫혔다.

| # | 변경 | 근거 |
|---|---|---|
| 1 | **사전 648개 확정.** N5~N1 5단계, 불변식 8종 검증 통과 | §4 |
| 2 | `ruby` 플래그 추가 — **104개가 가나 전용** | §4.2 |
| 3 | **`match` 표면형 배열 도입** (총 680개 = 대표 648 + 별칭 32) | §4.3 |
| 4 | 공백 표제어 10개 — **제외 결정을 뒤집어 유지 + 붙여 쓴 형태 추가** | §4.3-b |
| 5 | 원본 다의 표제어에서 **동의어 22개 회수** (생선·빨래·찻집 …) | §4.3-c |
| 6 | 부분 문자열 쌍 **74건** 자동 산출 → 테스트가 파일로 소비 | §4.5 |
| 7 | `matcher.js`가 `korean`이 아니라 `match` 배열을 소비하도록 명세 변경 | §5.2 |
| 8 | 미결 8건 → **3건** | §12 |

**산출물**

```
data/dictionary.json        646 엔트리 · 사람이 편집하는 정본
data/dictionary.js          window.KOJA_DICT · 확장이 읽는 번들 (자동 생성)
data/substring-pairs.json   74쌍 · 테스트가 읽는 최장 일치 케이스 (자동 생성)
tools/build_dictionary.py   검증 + 빌드. 위반 시 파일을 쓰지 않고 종료 코드 1
```

## 이 문서의 출처

| 출처 | 주 기여 |
|---|---|
| **A안** (KoJa) | 사전 648개 + 빌드 검증, 레벨·밀도 컨트롤, **결정적 동작**, 발표 대본형 성공 기준, `node --test` |
| **B안** (깊이 읽기) | 이론적 근거(Nation 커버리지), 후리가나 논거, 한자어 보너스·거짓짝, 정직한 포지셔닝, 경쟁 지형 |
| **C안** (v1.1) | 원본 데이터 실측, 조사 매칭 규칙, **MV3 실전 함정**, 3일 실행 계획 |

> **B안에 대한 단서.** B안은 같은 제품의 다른 버전이 아니라 **반대 방향의 다른 제품**이다
> (일본어 원문 → 한국어, 형태소 분석기 + Chrome Translator API + 뉘앙스 사전). 3인 3일에 들어가지
> 않는다. **기능은 전부 기각하고 판단·근거·정직성만** 가져왔다. 기각한 기능은 부록 C(v2 로드맵)에
> 보관했다 — 발표에서 "다음 단계는?"에 답할 재료다.

---

## 0. 세 문서가 충돌한 지점과 결정

### 0.1 후리가나를 화면에 표시할 것인가 — **표시한다 (기본 켜짐, 토글 제공)**

- **A안**: 한자만 표시, 후리가나는 툴팁에만. 근거는 "툴팁이 상시 참조 도구가 되면 번역 경로를
  강화해 목표를 배반한다."
- **결정**: **후리가나는 화면에, 한국어 뜻은 툴팁에만.**

A안의 원칙은 옳지만 겨냥한 대상이 다르다. 번역 경로를 강화하는 것은 **한국어 뜻**이지 **읽기**가
아니다. B안 9.6이 결정적 근거를 준다:

> 한국인은 「剣」의 뜻이 "검"임을 안다. 그러나 「けん」이라고 읽는 법은 모른다.
> 뜻만 기록하면 사용자는 일본어를 **한국식 한자로 훑는** 습관이 굳는다.

한자만 노출하면 한국인 학습자는 한자를 한국 한자음으로 읽고 넘어간다. 노출량은 쌓이는데 일본어는
하나도 늘지 않는다. 후리가나는 뜻을 가리지 않고 **읽으려 하지 않아도 눈에 들어온다** — 이 확장이
노리는 우연적 학습과 같은 원리다. A안의 우려는 **토글 ②(후리가나 끄기)**로 흡수한다.

### 0.2 `MutationObserver`를 쓸 것인가 — **쓴다. 단, 지연 스캔을 먼저 완성한 뒤**

3회 지연 스캔(로드 + 1s + 2s)이 기본 경로이고 D1에 완성한다. `MutationObserver`는 D2에 그 위에
얹는다. 무한 루프나 성능 문제가 보이면 `USE_OBSERVER = false` 한 줄로 즉시 되돌린다.

A안이 옳다 — 무한 스크롤에서 아무 일도 안 일어나면 데모가 약해진다. 다만 관찰자 무한 루프는
페이지를 얼어붙게 만들 수 있는 유일한 버그이므로 **끌 수 있는 형태로** 만든다.

### 0.3 사전 — **A안의 648개로 확정. 감사 통과.**

원본 `jlpt_n5_nouns.json`(403개)과 대조해 A안이 밝힌 제외 근거를 재현했고, 전부 맞았다.

| A안의 주장 | 실측 | 판정 |
|---|---|---|
| N5 정제 298개 (403 − 105) | 403개 확인 | 산술 일치 |
| 숫자 `四 七 九 十 零 ゼロ` 제외 | 6건 전부 원본에 존재 | 일치 |
| 위치 부사 `上 下 中 外 前 後 隣 横` 제외 | 8건 전부 존재 | 일치 |
| `english` 필드 오류 | `コップ→cop`, `バス→bass`, `パン→pan`, `フォーク→folk` **4건 확인** | 일치 |
| 한국어 키 중복 제거 | C안 분석의 충돌 27종과 일치 | 일치 |
| 거짓짝 제거 (遠慮·以上·撤退) | 셋 다 부재. `手法→기법`, 대체어 `予定·予約·展望` 존재 | 일치 |

**추가로 통과한 검사** (v2.0에서 `[확인 필요]`로 남겼던 것):

- 한국어 표제어 중복 **0건**
- 한 글자 표제어 **0건**
- 빈 값·앞뒤 공백 **0건**
- 복수 뜻(`"발, 다리"`)·괄호 표제어 **0건** ← v2.0의 최대 우려. **이미 처리되어 있었다**
- 일본어 표기 중복 **0건**
- `reading` 전부 가나
- 사전 개수 **648** 확정 (A안 문서의 "498" 표기는 오기)

### 0.4 치환량을 사용자가 조절하게 할 것인가 — **한다**

레벨 필터 + 밀도 슬라이더를 채택한다.

- **시연에서 가장 강한 장면이다.** 슬라이더를 움직이면 화면의 일본어가 눈앞에서 늘고 준다
- **B안 10.6이 학술 근거를 준다.** Nation의 연구에서 읽기를 통한 우연적 어휘 습득에는
  **아는 단어 커버리지 95~98%**가 필요하다. 밀도 슬라이더가 하는 일이 정확히 이 임계선을
  사용자가 직접 정하게 하는 것이다

---

## 1. 제품 정의

일본어 입문 학습자는 단어를 「経済 → 경제 → 의미」라는 **번역 경로**로 이해한다.
목표는 이 중간 단계를 없애고 단어를 모국어처럼 직관적으로 받아들이게 하는 것이다.

접근은 **노출량**이다. 따로 시간을 내서 외우는 대신, 매일 하는 일반 웹서핑 중에 한국어 웹페이지의
일부 명사를 일본어로 치환해 반복 노출시킨다.

**설계 원칙:** 툴팁은 막힐 때 확인하는 안전망이며, 상시 참조 도구가 되면 번역 경로를 강화해 목표를
배반한다. 툴팁 정보량을 최소로 유지하는 이유가 여기 있다.

### 1.1 정직한 포지셔닝

이 확장에는 학습 기록·복습·통계가 없다(§3.5). 그러므로 **"언어 학습 앱"으로 말하지 않는다.**

> **학습은 약속이 아니라 부작용으로 말한다.**

심사에서 "이게 학습을 어떻게 돕는가"라는 질문에 대한 첫 번째 방어선이다. 추적도 복습도 없는
제품을 학습 앱으로 내세우면 그 질문에 질 수밖에 없다. 대신 이렇게 말한다:

- 이 확장이 노리는 것은 **우연적 학습(incidental learning)** 이다. 의도적 암기가 아니라, 이미
  완전히 이해한 문맥(모국어 문장) 안에서 반복 마주치는 것으로 형태–의미 연결을 만든다
- 문맥 이해에 인지 자원을 쓰지 않으므로 학습자는 **형태와 읽기**에만 집중할 수 있다
- 밀도 슬라이더는 Nation의 95~98% 커버리지 임계선을 사용자가 직접 조절하는 장치다
- 기록·복습은 다음 단계의 자연스러운 확장이며 이번 3일에서 **의도적으로** 제외했다

### 1.2 한자어 보너스 — 왜 하필 한국어↔일본어인가

한국인은 **한자어를 이미 안다.** 「時間」「学校」「安全」은 배우지 않고도 뜻이 추론된다.
따라서 시작 시점은 "0% 이해"가 아니라 **"한자 명사의 상당 비율을 이미 읽는 상태"**다.

- 첫날부터 치환된 단어의 상당수가 즉시 이해된다. "쓸모없는 낙서" 구간이 없다
- **이 보너스는 한국어·중국어 사용자에게만 존재한다.** 영어권 사용자용 도구는 활용할 수 없다

그 청구서는 **거짓짝(false friend)** 이다 — 「勉強」은 면강이 아니라 공부, 「大丈夫」는 대장부가
아니라 괜찮다. 사용자는 **확신하고 틀리며 틀렸다는 신호를 받지 못한다.**
사전 단계에서 이미 처리했다(§4.6).

---

## 2. 사용자 · 문제 · 목표

| 항목 | 내용 |
|---|---|
| 최종 사용자 | 일본어 입문 학습자 (JLPT N5~N4) |
| **이번 3일의 실제 사용자** | **심사자(교수 / 심사위원 / 발표 관객)** |
| 문제 | 단어를 번역을 거쳐야 이해하게 되어 직관적으로 받아들이지 못함 |
| 목표 | 일반 웹서핑 중 반복 노출로 단어를 번역 없이 인지하게 함 |
| 사용 맥락 | **특정 사이트 전용이 아님.** 블로그·위키·커뮤니티·뉴스 등 평소 보는 한국어 페이지 |
| 방향 | 한국어 페이지의 한국어 명사 → 일본어 명사 (역방향은 v2) |

**심사자가 사용자라는 것의 함의**: 웹스토어 등록·심사 대응·개인정보처리방침·온보딩·회원가입이
전부 불필요하다. 설치 직후 바로 동작해야 한다. 대신 **어디서 시연하는가**가 최대 리스크다(§9).

---

## 3. 기능 요구사항

### F1. 단어 자동 치환

- 한국어 페이지의 텍스트에서 사전 표면형과 일치하는 명사를 일본어로 치환
- 표기는 **한자 + 후리가나 ruby**. `ruby: false`인 104개는 표기만 (§4.2)
- 치환된 단어는 시각적으로 구분 (점선 밑줄, 원문 스타일 훼손 최소화)
- **단어당 첫 등장만** 치환한다. 페이지별 "이미 치환한 엔트리 id" Set을 유지하고,
  재스캔 사이에도 유지, **끄기→켜기 시에는 초기화**

### F2. 난이도 컨트롤 2개

1. **레벨 필터**: N5~N1 중 선택. **누적 적용** (N3 선택 = N5+N4+N3, 527개)
2. **밀도 슬라이더**: 매칭된 후보 중 몇 %를 실제로 치환할지

> **결정적 선택 필수.** 밀도 선택을 무작위로 하면 새로고침마다 결과가 달라져 발표 대본이
> 재현되지 않는다. **같은 페이지 → 항상 같은 결과.**
> 구현: 후보 목록에서 균등 간격 추출 — `index % step === 0`

저장하는 상태는 설정뿐이고, **화면은 매번 결정론적으로 재계산된다.**

### F3. 마우스 오버 툴팁

- 내용은 **원본 한국어 단어 + 후리가나** 두 줄. 그 외 정보 없음
- 한국어는 **페이지에서 실제로 매칭된 표면형**을 보여준다 (별칭이면 별칭 그대로 — §4.3)
- 반응 지연 없이 즉시 표시. 재사용 `div` 하나 + **이벤트 위임**(단어마다 리스너 금지)
- 뷰포트 경계에서 잘리지 않도록 위/아래·좌/우 플립. `z-index: 2147483647`
- **표시하지 않는 것**: 영어 뜻(원본에 오류 4건 확인), 로마자(데이터에 없음),
  예문·품사·JLPT 배지(§1 정보량 최소 원칙)

### F4. 켜기/끄기 · 후리가나 토글

- 팝업에 토글 2개: ① 전체 on/off ② 후리가나 표시 on/off. **기본값 둘 다 켜짐**
- off 시 **즉시** 원본 텍스트로 복원. 새로고침을 요구하지 않는다
- 후리가나 토글은 CSS 클래스 하나로 즉시 반영
- 설정은 `chrome.storage.local`에 `{ enabled, furigana, level, density }`
- 팝업 ↔ 콘텐츠 스크립트 통신은 **`chrome.storage.onChanged` 구독으로 단일화**.
  `chrome.tabs.sendMessage`는 쓰지 않는다

### 3.5 범위 밖

| 제외 | 비고 |
|---|---|
| 학습 기록 · 복습(SRS) · 통계 · 단어장 저장 | §1.1의 포지셔닝으로 방어 |
| 툴팁의 예문 · 품사 · 배지 · 로마자 · 영어 뜻 | §1 정보량 최소 원칙 + 데이터 문제 |
| 일본어 → 한국어 역방향, 일본어 사이트 | **B안 전체가 여기 해당** → 부록 C |
| 번역 API · LLM **런타임 호출** | 사전은 전량 로컬. 개발 중 데이터 생성 보조는 허용 |
| 형태소 분석기 라이브러리 | 번들 용량·학습 비용이 3일에 감당 불가 |
| 사용자 사전 편집 UI · 별도 옵션 페이지 | 팝업으로 대체 |
| 사이트별 허용/금지 목록 | |
| 크롬 웹스토어 배포 · 아이콘 에셋 제작 | 압축해제 로드로 발표 |
| Firefox · Edge · 모바일 · PDF | Chrome만 |

---

## 4. 사전 — 최종 확정

### 4.1 구성

`data/dictionary.json` **646 엔트리 / 677 매칭 표면형**.

| 레벨 | 엔트리 | 누적 (레벨 필터가 이 값으로 동작) |
|---|---|---|
| N5 | 327 | 327 |
| N4 | 105 | 432 |
| N3 | 95 | 527 |
| N2 | 70 | 597 |
| N1 | 49 | **646** |

`ruby` 대상 **542** / 가나 전용 **104**. 번들 크기 `dictionary.js` **82.4 KB**.

> 누적 값은 `src/popup.html`의 레벨 `<option>` 표기와 같아야 한다.
> `tests/dictionary.test.mjs`가 이 정합을 검사한다.

### 4.2 스키마

```json
{
  "id": 298,
  "kanji": "水",
  "reading": "みず",
  "korean": "물고기",
  "level": "N5",
  "pos": "noun",
  "ruby": true,
  "match": ["물고기", "생선"]
}
```

| 필드 | 용도 |
|---|---|
| `kanji` | **화면에 넣을 일본어 표기.** 한자가 아닐 수 있다 (가타카나 104개) |
| `reading` | 후리가나 · 툴팁 2행 |
| `korean` | 대표 표제어. 사전 내 유일 |
| `ruby` | `false`면 `<ruby>` 없이 `kanji`만 넣는다 |
| `match` | **페이지에서 찾을 표면형 목록.** 항상 `korean`을 포함한다 |

**`ruby` 플래그가 필요한 이유** — 원본의 `kanji` 필드는 비어 있는 적이 없고, 가타카나 단어는
여기에 `バス`가 그대로 들어 있다(**646개 중 104개**). 한자 유무로 판정해야
`<ruby>バス<rt>バス</rt></ruby>`처럼 같은 글자를 두 번 쓰는 일이 없다.

```js
entry.ruby = /[\u4E00-\u9FFF]/.test(entry.kanji);
```

### 4.3 `match` 표면형 — 대표 646 + 별칭 31

한 엔트리가 여러 한국어 표면형으로 매칭될 수 있다. **"한국어 표제어 중복 0건" 불변식은
그대로 유지된다** — 별칭은 새 엔트리가 아니라 기존 엔트리의 추가 진입점이다.

**(a) 대표 표제어 646개** — `korean` 필드 그대로.

**(b) 공백 표제어의 붙여 쓴 형태 10개** — **v2.0의 "제외" 결정을 뒤집었다.**

v2.0은 공백 표제어를 "단일 토큰 매처로는 매칭 불가"를 이유로 제외하기로 했다.
**그 이유가 §5.2의 정규식 설계에는 해당하지 않는다** — 교체 후보 안에서 공백은 그냥 리터럴
문자이고, 경계 검사도 정상 동작한다. 기술적 장애물이 없는데 N5 단어 10개를 버릴 이유가 없다.

대신 반대 문제가 남는다: 페이지에 「이번달」로 붙여 쓰여 있으면 「이번 달」과 매칭되지 않는다.
한국어 웹 글에서는 붙여 쓴 형태가 오히려 흔하다. 그래서 둘 다 넣는다.

| 표제어 | 추가 표면형 | | 표제어 | 추가 표면형 |
|---|---|---|---|---|
| 오늘 아침 | 오늘아침 | | 매일 아침 | 매일아침 |
| 오늘 밤 | 오늘밤 | | 매일 밤 | 매일밤 |
| 이번 달 | 이번달 | | 다음 달 | 다음달 |
| 이번 주 | 이번주 | | 다음 주 | 다음주 |
| 한 달 | 한달 | | 두 사람 | 두사람 |

**(c) 원본 다의 표제어에서 회수한 동의어 22개** — 원본에는 `"물고기, 생선"`처럼 한 항목에
여러 한국어 뜻이 들어 있었다. 사전을 만들 때 앞의 하나만 남기고 나머지를 버렸는데, 버려진 쪽도
페이지에 실제로 등장한다. 아래 조건을 전부 만족하는 것만 되살렸다.

> 2글자 이상 · 공백 없음 · 다른 표제어와 충돌 없음 · C안의 오탐 위험 목록에 없음
> · 날짜/숫자 계열 아님

| 일본어 | 대표 | 별칭 | | 일본어 | 대표 | 별칭 |
|---|---|---|---|---|---|---|
| 魚 | 물고기 | 생선 | | 庭 | 정원 | 마당 |
| 洗濯 | 세탁 | 빨래 | | 角 | 모퉁이 | 모서리 |
| 買い物 | 쇼핑 | 장보기 | | 黒 | 검정색 | 검은색 |
| 上着 | 겉옷 | 상의 | | 休み | 휴식 | 방학 |
| 喫茶店 | 카페 | 찻집 | | 姉 | 언니 | 누나 |
| お風呂 | 목욕 | 욕조 | | 伯母さん | 아주머니 | 이모, 고모 |
| ノート | 노트 | 공책 | | おじさん | 아저씨 | 삼촌 |
| ボタン | 버튼 | 단추 | | 奥さん | 부인 | 사모님 |
| レコード | 음반 | 레코드 | | 始め | 시작 | 처음 |
| テスト | 시험 | 테스트 | | 暇 | 여가 | 한가함 |
| ちゃわん | 밥그릇 | 찻잔 | | | | |

**툴팁에는 실제로 매칭된 표면형을 보여준다.** 페이지에서 「생선」을 잡았으면 툴팁 1행은
「물고기」가 아니라 **「생선」**이다. 사용자가 방금 본 단어와 툴팁이 어긋나면 안 된다.

### 4.4 정제에서 제외한 것

| 유형 | 예시 |
|---|---|
| 대명사·의문사 | あなた, いつ, どこ, いくら, こっち/そっち/あっち |
| 형용사·형용동사 | 嫌い, 好き, 上手, 下手, 元気 |
| 부사류 | 本当, たくさん, 一番, 一緒, みんな, 全部 |
| 날짜 (N일) | 一日, 二日 … 二十日 |
| 숫자 | 四, 七, 九, 十, 零, ゼロ |
| 위치 부사 | 上, 下, 中, 外, 前, 後, 隣, 横 |
| **한 글자 표면형 전량** | 비, 집, 개, 이, 말, 차, 물, 길, 밤, 책, 손, 꽃, 눈 … |
| 데이터 오류 | そば (側「근처」와 蕎麦「국수」를 한 항목에 병합) |
| 한국어 키 중복 | 車/自動車, 辞書/字引, お手洗い/トイレ, 学生/生徒, 服/洋服 |

**한 글자를 전량 제외한 이유**: 한국어 1음절 어휘는 조사·어미·의존명사와 충돌한다.
「이」(歯)는 조사 「이」와, 「안」(中)은 부정 부사와, 「개」(犬)는 「3개」의 의존명사와,
「말」(言葉)은 「말하다」 어간과 겹친다. **오탐 하나가 데모를 망친다.**
불변식으로 강제하므로 별칭에도 한 글자는 들어갈 수 없다. 나중에 화이트리스트로 개별 복원할 수 있다.

### 4.5 빌드가 강제하는 불변식 8종

`tools/build_dictionary.py`가 검사하고, **하나라도 위반하면 파일을 쓰지 않고 종료 코드 1**로 끝난다.

| # | 불변식 |
|---|---|
| 1 | 한국어 표제어 중복 0 |
| 2 | 일본어 표기 중복 0 |
| 3 | 빈 값 · 앞뒤 공백 0 |
| 4 | `reading`이 가나만 |
| 5 | `korean`이 한글(+공백)만 |
| 6 | 복수 뜻(`,`)·괄호 표제어 0 |
| 7 | `ruby` 플래그가 `kanji` 필드와 정합 |
| 8 | `match` 배열이 `korean`을 포함 · 한 글자 표면형 0 · **표면형 전역 충돌 0** |

동시에 두 파일을 생성한다.

```
$ PYTHONIOENCODING=utf-8 python tools/build_dictionary.py
OK — 불변식 8종 전부 통과
  엔트리      : 646  (N5 327 / N4 105 / N3 95 / N2 70 / N1 49)
  매칭 표면형 : 677  (대표 646 + 별칭 31)
  ruby 대상   : 542  / 가나 전용 104
  → data/dictionary.js (82.4 KB)
  → data/substring-pairs.json

부분 문자열 쌍 74 건 — 최장 일치가 긴 쪽을 먼저 잡아야 하는 지점
```

**부분 문자열 쌍 74건은 오류가 아니라 테스트 케이스다.** `data/substring-pairs.json`으로
떨어지고 `tests/matcher.test.mjs`가 그대로 읽는다. 사전이 바뀌면 테스트 케이스도 자동으로 따라온다.

가장 실수하기 쉬운 것 — 왼쪽부터 스캔하며 각 위치에서 최장을 찾지 않으면 깨진다:

| 위험 쌍 | 잘못 구현하면 |
|---|---|
| 「주머니」⊂「아주머니」 | 아주머니 → 「아」+ポケット |
| 「아버지」⊂「할아버지」 | 할아버지 → 「할」+お父さん |
| 「나이」⊂「나이프」 | 나이프 → 年+「프」 |
| 「수요」⊂「수요일」 | 수요일 → 需要+「일」 |
| 「화장」⊂「화장실」 | 화장실 → 化粧+「실」 |
| 「고기」⊂「물고기·소고기·닭고기·돼지고기」 | 물고기 → 「물」+肉 |

전체 74쌍은 부록 A.

### 4.6 거짓짝(false friend) 제거

한자를 그대로 옮기면 뜻이 달라지거나 한국어 동음이의어와 충돌하는 것을 뺐다. 부재를 검사로 확인했다.

| 후보 | 문제 | 대체 |
|---|---|---|
| 遠慮 | 「사양」이 仕様(스펙)과 동음 | 予定 (예정) |
| 以上 | 「이상」이 異常과 동음 | 予約 (예약) |
| 撤退 | 「철수」가 사람 이름과 충돌 | 展望 (전망) |

`手法`은 「수법」이 한국어에서 부정적 함의를 가지므로 「기법」으로 넣었다.

### 4.7 데이터 정직성 — 발표에 반드시 포함

JLPT는 **2010년부터 공식 단어 목록을 공개하지 않는다.** 시중 "N3 단어장"은 모두 출판사·커뮤니티
추정판이며 서로 다르다. 따라서 이 확장의 등급 표기는 **채택한 목록 기준의 추정치**이며 공인 등급이
아니다. N4~N1 320개는 웹서핑 빈출 어휘를 기준으로 팀이 선정했다.

**저작권 처리**: 오픈소스 저장소의 어휘 목록을 *선정 참고용*으로만 봤고, 일본어 표기·후리가나·
한국어 표제어·레벨은 전부 직접 작성했다. 해당 저장소는 라이선스가 없어 데이터 복사·재배포
권한이 없기 때문이다. **어떤 단어가 학습에 유용한지에 대한 판단은 저작물이 아니다.**

---

## 5. 매칭 규칙 (`matcher.js`)

**브라우저용 한국어 형태소 분석기는 없다.** 조사 목록 기반 어절 끝 처리로 우회한다.

`matcher.js`는 **DOM을 모르는 순수 함수**여야 한다. 입력은 문자열, 출력은 매치 목록.
이 프로젝트에서 버그가 가장 나기 쉬운 부분이고, 분리해 두면 `node --test`에서 바로 검증된다.

### 5.1 필수 규칙 5개

| # | 규칙 | 이유 |
|---|---|---|
| 1 | 표면형 **최장 일치 우선** | 「아주머니」가 「주머니」로 쪼개지면 안 된다. 74쌍(§4.5) |
| 2 | 조사도 **긴 것부터** 매칭 | 「에서」를 「에」보다 먼저 봐야 「경제에서」가 올바르게 잘린다 |
| 3 | 후보 **앞 글자가 한글·숫자·영문이면 제외** | **숫자를 빠뜨리면 「2시간」이 「2」+時間으로 치환된다** |
| 4 | 조사까지 포함한 **뒷 글자가 한글·숫자·영문이면 후보 폐기** | 「경제학」을 걸러낸다. **조사를 뗀 짧은 후보로 되돌아가 재시도하지 않는다** |
| 5 | 한 글자 표면형은 사전 단계에서 제외 | §4.4 · 불변식 8 |

```js
const BOUNDARY_BAD = /[가-힣ㄱ-ㅎㅏ-ㅣ0-9A-Za-z]/;   // 양옆에 오면 매칭 실패
```

규칙 3의 숫자 조건은 C안이 원본 데이터로 찾아낸 것이다. 한 글자를 전량 제외해도
**2글자 이상에서 여전히 발생한다** — 「2시간」「3주말」「10년간」은 흔하다.

### 5.2 구현 — 정규식 1개로 충분

677개 표면형 규모에서 성능 문제는 없다. **`korean`이 아니라 `match` 배열을 펼쳐서 쓴다.**

```js
const ORDER = { N5: 0, N4: 1, N3: 2, N2: 3, N1: 4 };

function compile(entries, maxLevel) {
  const pairs = [];                          // [표면형, 엔트리]
  for (const e of entries) {
    if (ORDER[e.level] > ORDER[maxLevel]) continue;   // 레벨 누적 필터
    for (const m of e.match) pairs.push([m, e]);
  }
  pairs.sort((a, b) => b[0].length - a[0].length);    // 최장 우선 (JS 교체는 leftmost-first)

  const JOSA = ['에서는','으로는','에게는','에게서','으로써','으로서','이라고','부터는','까지는',
                '에서','에게','한테','으로','이랑','라고','까지','부터','조차','마저','처럼',
                '보다','밖에','만큼','이나','이란',
                '와','과','은','는','이','가','을','를','에','도','만','로','의','랑','나']
               .sort((a, b) => b.length - a.length);

  const re = new RegExp(
    '(?<![가-힣ㄱ-ㅎㅏ-ㅣ0-9A-Za-z])' +
    '(' + pairs.map(p => p[0]).join('|') + ')' +
    '(' + JOSA.join('|') + ')?' +
    '(?![가-힣ㄱ-ㅎㅏ-ㅣ0-9A-Za-z])',
    'g'
  );
  return { re, index: new Map(pairs) };
}
```

기대 API (테스트가 정의한다, §11):

```js
findMatches(text, entries, { maxLevel }) -> [{ start, length, surface, entry }]
```

`surface`를 돌려주는 것이 중요하다 — 툴팁이 **실제로 매칭된 표면형**을 보여야 하기 때문이다(§4.3).

### 5.3 착수 전 통과시킬 케이스

```
'경제가 나빠졌다'   → 경제/가
'경제학 개론'      → 매칭 없음      (규칙 4)
'신경제 정책'      → 매칭 없음      (규칙 3, 앞 글자 '신')
'2시간 걸렸다'     → 매칭 없음      (규칙 3, 앞 글자 '2')
'아주머니가 왔다'   → 아주머니/가    (규칙 1)
'할아버지에게'     → 할아버지/에게   (규칙 1)
'수요일에 만나자'   → 수요/일 아님, 수요일/에  (규칙 1)
'물고기를 잡았다'   → 물고기/를      (규칙 1, 「고기」로 쪼개지지 않음)
'생선을 구웠다'    → 생선/을 → 魚    (별칭, §4.3-c)
'이번달 목표'      → 이번달 → 今月   (붙여 쓴 형태, §4.3-b)
'다음 주에 보자'   → 다음 주/에     (「다음」으로 쪼개지지 않음)
```

`data/substring-pairs.json`의 74쌍은 전부 "긴 쪽이 이긴다"를 검사하는 케이스로 자동 전개한다.

---

## 6. 치환 범위 — 안전장치

### 6.1 제외 요소 (필수)

```
script, style, noscript, pre, code, kbd, samp,
textarea, input, select, option, button, nav,
svg, math, ruby, rt, [contenteditable], [aria-hidden="true"],
.koja-word                      ← 자기 자신 재치환 방지
```

`textarea` / `input` / `contenteditable` 제외는 **안전상 필수**다. 일반 웹서핑이 대상이라
로그인 폼·검색창·댓글창을 항상 만난다. 사용자가 입력한 텍스트가 일본어로 바뀌어 전송되면
사고가 된다.

### 6.2 본문 영역 우선 탐색

전체 `body`를 훑지 않고 본문을 먼저 찾는다. 못 찾으면 `body`로 폴백하되, 6.1의 제외 규칙이
안전망 역할을 한다.

| 사이트 | 선택자 |
|---|---|
| `news.naver.com` | `#dic_area`, `#newsct_article` |
| `ko.wikipedia.org` | `.mw-parser-output` (추가 제외: `.infobox`, `.navbox`, `.reference`, `.mw-editsection`, `#toc`, `.hatnote`, `table`) |
| 자체 샘플 페이지 | `#koja-demo-body` |
| 그 외 | `article` → `main` → `[role="main"]` → `body` |

---

## 7. 동적 콘텐츠

```js
// 1단계 (D1, 항상 동작) — 로드 직후 + 1s + 2s, 총 3회
// 2단계 (D2, 끌 수 있음) — MutationObserver, 300ms 디바운스
const USE_OBSERVER = true;   // 문제가 생기면 false 한 줄로 1단계만 남는다
```

- 추가된 노드만 처리한다. 확장이 삽입한 `span`은 관찰 대상에서 건너뛴다(**무한 루프 방지**)
- 재스캔 사이에도 "이미 치환한 엔트리 id" Set을 유지한다
- `run_at: "document_idle"`

---

## 8. 기술 설계

### 8.1 파일 구조

```
manifest.json                    MV3
data/dictionary.json             646 엔트리 · 정본 (사람이 편집)
data/dictionary.js               window.KOJA_DICT (자동 생성 · 편집 금지)
data/substring-pairs.json        74쌍 (자동 생성)
src/matcher.js                   사전 매칭 + 조사 처리 (순수 함수, 테스트 대상)
src/scope.js                     본문 영역 결정 + 제외 규칙
src/replacer.js                  텍스트 노드 치환 · 원문 보관 · 복원
src/tooltip.js                   툴팁
src/content.js                   오케스트레이션 · 지연 재스캔 · MutationObserver
src/content.css                  ruby · 툴팁 · 하이라이트
src/popup.html / popup.js        레벨 · 밀도 · 토글 2개
tools/build_dictionary.py        검증 + 빌드
tests/dictionary.test.mjs        사전 불변식
tests/matcher.test.mjs           매칭 명세
demo/sample.html                 자체 데모 페이지
```

```json
{
  "manifest_version": 3,
  "name": "KoJa",
  "version": "0.1.0",
  "permissions": ["storage"],
  "action": { "default_popup": "src/popup.html" },
  "content_scripts": [{
    "matches": ["<all_urls>"],
    "js": ["data/dictionary.js", "src/matcher.js", "src/scope.js",
           "src/replacer.js", "src/tooltip.js", "src/content.js"],
    "css": ["src/content.css"],
    "run_at": "document_idle"
  }]
}
```

### 8.2 치환 파이프라인

```
1. scope.js       본문 루트 결정 (§6.2)
2. TreeWalker     텍스트 노드 순회, 제외 영역 건너뛰기 (§6.1)
3. matcher.js     레벨 필터 → 최장 일치 + 조사 접미 (§5)
4. 밀도 선택      index % step === 0  ← 결정적 (§F2)
5. 첫 등장 검사   이미 치환한 엔트리 id면 스킵
6. replacer.js    노드 분할 후 span 삽입
7. tooltip.js     document 레벨 이벤트 위임
```

### 8.3 MV3 실전 함정 — 착수 전 전원 필독

세 SPEC 중 C안에만 있던 부분이다. 각각 반나절을 태울 수 있다.

**(1) 선언형 콘텐츠 스크립트는 ES 모듈을 지원하지 않는다.**
`import`/`export`를 쓰면 조용히 아무것도 안 된다. `js` 배열에 **순서대로** 나열하고 공유 전역에 붙인다.

```js
window.KOJA = window.KOJA || {};
KOJA.matcher = { findMatches };
```

**(2) 사전은 JSON이 아니라 `.js`로 번들한다.**
`fetch(chrome.runtime.getURL(...))`은 `web_accessible_resources` 선언이 필요하고 비동기이며
실패 분기를 만든다. `build_dictionary.py`가 `.json`과 `.js`를 **동시에** 쓴다 —
테스트는 `.json`을, 확장은 `.js`를 읽는다.

**(3) `file://` 페이지에서는 콘텐츠 스크립트가 기본적으로 실행되지 않는다.**
`demo/sample.html`을 파일 URL로 열 계획이라면 `chrome://extensions` → 해당 확장 →
**"파일 URL에 대한 액세스 허용"**을 켜야 한다. 안 켜면 **아무 일도 일어나지 않는다.**
심사 당일 이걸 모르면 지지대로 삼은 샘플 페이지가 제일 먼저 무너진다.
대안(권장): `python -m http.server 8000` → `http://localhost:8000/demo/sample.html`

**(4) 코드 수정 후 확장 새로고침 + 페이지 새로고침 둘 다.**
확장만 새로고침하면 이미 열린 탭에는 옛 스크립트가 남는다. "고쳤는데 안 바뀐다"의 90%가 이것이다.

### 8.4 치환 마크업과 복원

```html
<span class="koja-word" data-ko="생선" data-kana="さかな"><ruby>魚<rt>さかな</rt></ruby></span>
<span class="koja-word" data-ko="버스" data-kana="バス">バス</span>   <!-- ruby: false -->
```

- `data-ko`에는 **페이지에 실제로 있던 표면형**을 넣는다. 복원과 툴팁 둘 다 이 값을 쓴다
- off 시 `span`을 텍스트 노드로 되돌려 **무손실 복원**
- **복원 후 `parentNode.normalize()`를 호출한다.** 안 하면 켜기/끄기를 반복할수록 DOM이
  파편화되고 어절 경계가 어긋난다

```css
.koja-word      { border-bottom: 1px dotted currentColor; }
.koja-word ruby { ruby-position: over; line-height: 1; }
.koja-word rt   { font-size: .55em; line-height: 1; display: revert; opacity: 1; visibility: visible; }
html.koja-no-furigana .koja-word rt { display: none; }
```

### 8.5 에러 처리

- 각 스캔 사이클 전체를 `try/catch`로 감싼다. 한 사이트에서 실패해도 페이지가 멈추면 안 된다
- 본문을 못 찾으면 조용히 아무것도 하지 않는다 (콘솔 경고만)
- 치환 총 개수 상한 (페이지당 200개) — 초대형 문서 프리징 방지
- ~~사전 로드 실패 분기~~ → 8.3-(2)로 존재하지 않음

### 8.6 권한 경고

`<all_urls>` 때문에 설치 시 **"모든 웹사이트의 데이터를 읽고 변경할 수 있습니다"** 경고가 뜬다.
발표에서 선제 설명한다: 텍스트 치환에 필요하며, 어떤 데이터도 외부로 전송하지 않는다.
(선언형 콘텐츠 스크립트만 쓰면 `host_permissions`는 불필요하다.)

---

## 9. 성공 기준 — 발표 대본을 재현할 수 있으면 성공

**체크리스트가 아니라 대본이 기준이다.**

### 9.1 대본 6단계

1. 평소 보는 한국어 페이지를 원본 상태로 연다
2. 확장 on → 단어가 일본어로 바뀐다
3. 치환된 단어에 마우스 오버 → 원본 한국어 + 후리가나 툴팁
4. 레벨을 N5 → N3으로 올린다 → 치환 대상이 327개에서 527개로 늘어난다
5. 밀도 슬라이더를 조절한다 → 치환량이 눈앞에서 변한다
6. off → 원본으로 완전 복원

### 9.2 검증 조건

- [ ] 새로고침·재실행 시 **매번 동일한 단어가 치환된다** (결정적 동작)
- [ ] 서로 다른 유형의 사이트 **3곳 이상**에서 레이아웃이 깨지지 않는다
- [ ] **명백한 오치환이 0건이다.** 정확도가 개수보다 중요하다
- [ ] 화면 한 눈에 **8개 이상** 치환된다
- [ ] 입력창·검색창의 텍스트는 변형되지 않는다
- [ ] 복원 후 원본과 텍스트가 일치한다 (**3회 반복해도**)
- [ ] 후리가나 토글이 즉시 반영된다
- [ ] 무한 스크롤 페이지에서 새로 로드된 영역도 치환된다 (`USE_OBSERVER=true`일 때)
- [ ] 콘솔에 처리되지 않은 에러가 없다

### 9.3 시연 사이트 3곳

| # | 성격 | 비고 |
|---|---|---|
| 1 | 자체 샘플 페이지 (`demo/sample.html`) | **반드시 동작하는 지지대.** 8.3-(3) 주의 |
| 2 | 한국어 위키백과 문서 | 정적 · 대용량 텍스트 · 광고 없음 |
| 3 | 뉴스 기사 또는 블로그 글 | `[확인 필요]` — 로그인·광고가 적어 화면이 흔들리지 않는 곳 |

**시연할 페이지는 미리 확정하고 로컬에 HTML로 저장해 둔다.** 사이트 DOM이 바뀌거나 네트워크가
말썽일 때의 백업 시나리오다.

지정 3곳이 아닌 사이트에서 치환이 안 되거나 적게 되는 것은 실패가 아니다.
**다만 페이지가 깨지거나 멈추면 실패다.**

---

## 10. 3일 일정

**사전이 완성되어 있으므로 코드와 안정성에 시간을 쓸 수 있다.**
통합·디버깅에 전체의 1/3을 남긴다.

### D0 — 착수 전 20분 (전원)

- [ ] §0의 결정 4건과 §8.3의 함정 4건 함께 읽기
- [ ] `python tools/build_dictionary.py` 실행 → "불변식 8종 전부 통과" 확인
- [ ] 시연 페이지 3곳 확정 + 로컬 저장

### D1 — 병렬 개발

| 담당 | 산출물 | 완료 기준 |
|---|---|---|
| A | `matcher.js` | `matcher.test.mjs` 통과 (§5.3 케이스 + 74쌍) |
| B | `scope.js` + `replacer.js` + 3회 지연 스캔 | 위키백과에서 단어 1개 치환·복원 성공 |
| C | `manifest.json` + `popup.html/js` + `demo/sample.html` | 확장 로드 + 팝업이 storage에 기록 |

**D1 종료 조건: 확장이 로드되고 콘솔 에러가 0이다.** 여기 못 가면 D2에 기능을 줄인다.

### D2 — 통합 + UI

- 오전: A+B 통합 → `content.js` 오케스트레이션
- 오후: 레벨 필터 · 밀도 슬라이더(**결정적**) · 툴팁 · `content.css`
- 저녁: `MutationObserver` 추가 → 무한 스크롤에서 확인. **터지면 `USE_OBSERVER=false`**

### D3 — 정확도 + 리허설

- 오전: 3곳 실측 → 오탐 제거, 레이아웃 깨짐 수정
- 오후: §9 대본 6단계 **전체 2회** — 1회는 시연할 노트북 그대로
- 남는 시간: 발표 자료 (§1.1 포지셔닝 + §4.7 데이터 정직성 + §8.6 권한 설명)

### 10.1 기능을 줄여야 할 때의 순서

위에서부터 먼저 버린다.

1. `MutationObserver` (3회 지연 스캔만)
2. 밀도 슬라이더 (레벨 필터만 남김)
3. 후리가나 토글 (항상 켜짐 고정)
4. 툴팁 (치환만으로도 대본 1·2·6단계는 성립)

**절대 버리지 않는 것**: 입력창 보호(§6.1), off 시 무손실 복원(§8.4), 결정적 동작(§F2), 콘솔 에러 0.

---

## 11. 검증 · 자동 테스트

### 11.1 수동 검증 순서

1. `PYTHONIOENCODING=utf-8 python tools/build_dictionary.py` — 사전 검증 및 빌드
2. `chrome://extensions` → 개발자 모드 → 압축해제된 항목 로드
3. **"파일 URL에 대한 액세스 허용" 켜기** (또는 로컬 http 서버, §8.3-3)
4. 시연 사이트 3곳에서 §9.1 대본 6단계를 순서대로 실행
5. 새로고침 3회 후 치환된 단어 집합이 동일한지 확인 (**결정적 동작**)
6. on/off를 3회 반복한 뒤 본문 텍스트가 원문과 완전히 같은지 확인
7. 검색창에 한글 입력 후 텍스트가 변형되지 않는지 확인 (**안전성 회귀 테스트**)
8. 무한 스크롤 페이지를 끝까지 내려 추가 영역도 치환되는지 확인
9. 콘솔 에러 0건 확인

### 11.2 자동 테스트

`node --test` 로 실행한다 (**인자 없이** — 디렉터리를 주면 Node가 모듈로 로드하려 해서 실패한다).

| 파일 | 내용 |
|---|---|
| `tests/dictionary.test.mjs` | 사전 불변식 8종 (§4.5) — 빌드 스크립트와 같은 검사를 JS에서 재확인 |
| `tests/matcher.test.mjs` | 매칭 명세 (§5.3) + `data/substring-pairs.json` 74쌍 자동 전개 |

**`matcher.test.mjs`가 기대 API를 정의한다 — 구현 전에 먼저 읽는다.**
사전이 바뀌면 `substring-pairs.json`이 바뀌고 테스트 케이스도 자동으로 따라온다.

**Hook 연동**: `.claude/settings.json`의 `PostToolUse` 훅이 `src/`, `tests/`,
`data/dictionary.json`, `tools/*.py` 변경을 감지해 테스트를 자동 실행한다.
`tools/*.py`가 바뀌면 사전을 먼저 재생성하므로 스크립트와 JSON이 어긋나지 않는다.
실패하면 종료 코드 2로 결과를 되돌려 준다.

**구현 subagent**: `.claude/agents/koja-implementer.md` (Sonnet). 태스크 하나를 받아 SPEC·테스트를
읽고 구현한 뒤 `node --test`가 통과할 때까지 고친다. 실제로 터진 함정(최장 일치, 결정적 동작,
제외 영역, MutationObserver 루프, **경계 문자의 숫자 조건**, **`match` 배열 전개**)이
프롬프트에 들어 있고, 테스트·사전·SPEC 수정은 금지되어 있다.

---

## 12. 미결 사항 `[확인 필요]`

| # | 항목 | 필요 시점 | 추천안 |
|---|---|---|---|
| 1 | 시연 사이트 3번째 확정 | D0 | 광고·로그인이 적은 블로그 |
| 2 | 심사 평가 기준표의 존재와 항목 | **즉시** | 있다면 그것이 §9의 정답이 된다 |
| 3 | 확장 이름·아이콘 | D2 | 아이콘 없어도 로드는 된다 |

### 해소된 항목

| 항목 | 결론 |
|---|---|
| 사전 개수 648 vs 498 | **648** (빌드 출력이 정본, 문서 오기였음) |
| `ruby` 플래그 | 추가 완료. 가나 전용 **104개** |
| 복수 뜻 표제어 | **0건** — 이미 처리되어 있었다. 불변식 6으로 재발 방지 |
| 공백 표제어 10개 | **유지 + 붙여 쓴 형태 추가** (§4.3-b) |
| 팀 3인 역할 분담 | §10 D1 표 (로직 / DOM / 데이터·UI) |
| 한 글자 표면형 화이트리스트 | v1은 전량 제외. 불변식 8로 강제 |

---

## 13. 아직 해소되지 않은 반론

**답이 없거나, 답이 있어도 대가가 남는 것들이다.**
발표에서 먼저 말하면 방어가 되고, 지적당한 뒤에 말하면 변명이 된다.

### 13.1 복습 장치가 없으므로 습득 속도는 느리다

같은 표현을 **8~20회** 만나야 정착한다. 이 확장에는 그 반복을 보장하는 장치가 없다.
"단어당 첫 등장만" 규칙은 한 페이지 안의 반복까지 줄인다.

→ 결함이 아니라 **선택의 결과**이며, §1.1의 포지셔닝으로 처리한다. 학습은 약속이 아니라 부작용이다.

### 13.2 등급 표기는 추정치다

§4.7. JLPT 공식 목록이 존재하지 않으므로 "N3 단어"라는 표기 자체가 팀의 판단이다.
숨기지 않고 발표에 명시한다.

### 13.3 조사 목록 방식은 원리적으로 완전하지 않다

형태소 분석기 없이 어절 끝만 보는 방식은 「경제학」류를 규칙으로 걸러낼 뿐 의미를 모른다.
새로운 오탐 유형은 언제든 나올 수 있다. 한 글자 전량 제외(§4.4)는 가장 큰 오탐군을 통째로 없앤
실용적 타협이지, 문제를 푼 것이 아니다.

**남아 있는 잔여 위험 예시**: 「상당」(相当)은 「상당한/상당히」로만 쓰여 사실상 매칭되지 않고,
「해당」(該当)은 「해당 지역」에서 매칭되지만 의미상 문제없다. 이런 것들은 D2 실측으로만 걸러진다.

### 13.4 시장은 비어 있지 않다

- **Toucan** — 모국어 페이지의 일부 단어를 목표어로 치환. **이 아이디어의 직계**다
- **Yomitan** — 일본어 호버 사전을 이미 무료로 제공
- 그 외 Language Reactor, Readlang, LingQ, Migaku

"차별점이 뭐냐"는 질문은 나온다. 준비된 답은 **§1.2의 한자어 보너스** — 한국어 사용자에게만
존재하는 출발선이고, 영어권 사용자용 도구는 활용할 수 없다. 그리고 **레벨·밀도가 결정적으로
동작한다**는 것 — 같은 페이지가 항상 같게 보이는 도구는 의외로 드물다.

### 13.5 명사만 바꾸는 것으로 문장 이해가 늘지는 않는다

모르는 단어가 0개여도 못 읽는 문장이 있고, 명사 치환은 문장 구조를 전혀 건드리지 않는다.
**이 확장은 어휘 노출 도구이지 독해 도구가 아니다.**
1차 버전에서 유보는 가능하지만, 해결됐다고 착각하면 안 된다.

---

## 부록 A. 부분 문자열 쌍 74건 (`data/substring-pairs.json`)

최장 일치가 긴 쪽을 먼저 잡아야 하는 지점. **오류가 아니라 테스트 케이스다.**

| 짧은 쪽 ⊂ 긴 쪽 | | |
|---|---|---|
| 아침 ⊂ 아침밥 · 오늘아침 · 매일아침 · 오늘 아침 · 매일 아침 | 머리 ⊂ 머리카락 | 노래 ⊂ 노래방 |
| 영화 ⊂ 영화관 | 남자 ⊂ 남자아이 | 여자 ⊂ 여자아이 |
| 외국 ⊂ 외국인 | 회사 ⊂ 회사원 | 학생 ⊂ 유학생 · 대학생 · 중학생 · 고등학생 |
| 학교 ⊂ 초등학교 · 고등학교 | 오늘 ⊂ 오늘밤 · 오늘아침 · 오늘 밤 · 오늘 아침 | 작년 ⊂ 재작년 |
| 은행 ⊂ 은행원 | 그램 ⊂ 킬로그램 | 결혼 ⊂ 국제결혼 |
| 아이 ⊂ 남자아이 · 여자아이 | 시간 ⊂ 점심시간 | 셔츠 ⊂ 와이셔츠 |
| 대학 ⊂ 대학생 | 다음 ⊂ 다음달 · 다음주 · 다음 달 · 다음 주 | 동물 ⊂ 반려동물 |
| **나이 ⊂ 나이프** | 여름 ⊂ 여름방학 | **고기 ⊂ 소고기 · 물고기 · 닭고기 · 돼지고기** |
| 음료 ⊂ 음료수 | 사람 ⊂ 두사람 · 두 사람 | 점심 ⊂ 점심밥 · 점심시간 |
| 매일 ⊂ 매일밤 · 매일아침 · 매일 밤 · 매일 아침 | 가게 ⊂ 채소가게 | 미터 ⊂ 킬로미터 |
| 채소 ⊂ 채소가게 | 방학 ⊂ 여름방학 | 저녁 ⊂ 저녁밥 |
| 일본 ⊂ 일본어 · 일본인 | 한국 ⊂ 한국어 | 중국 ⊂ 중국어 |
| 소리 ⊂ 목소리 | 부모 ⊂ 부모님 | 경찰 ⊂ 경찰관 |
| 국제 ⊂ 국제결혼 | 생활 ⊂ 자취생활 | 졸업 ⊂ 졸업식 |
| 수영 ⊂ 수영장 | 일기 ⊂ 일기예보 | **수요 ⊂ 수요일** |
| **화장 ⊂ 화장실** | 지하 ⊂ 지하철 | **아버지 ⊂ 할아버지** |
| **주머니 ⊂ 아주머니** | 라디오 ⊂ 라디오카세트 | |

굵은 것이 실수하면 가장 눈에 띄는 케이스다.

## 부록 B. 세 문서에서 각각 살린 판단

| 판단 | 출처 |
|---|---|
| 결정적 동작 (`index % step`) — 발표 대본 재현의 전제 | A |
| 발표 대본 = 성공 기준 | A |
| 사전 빌드 스크립트가 불변식 강제 | A |
| 부분 문자열 쌍을 **테스트 케이스 생성기**로 쓰는 발상 | A |
| 한 글자 전량 제외 | A |
| 데이터 정직성 · 저작권 명시 | A |
| Nation 95~98% 커버리지 — 밀도 슬라이더의 근거 | B |
| 후리가나 병기 논거 ("한국식 한자로 훑는 습관") | B |
| 한자어 보너스 · 거짓짝 | B |
| 학습은 부작용으로 말한다 | B |
| "해소되지 않은 반론" 섹션 | B |
| 경계 문자에 숫자 포함 (「2시간」) | C |
| MV3 실전 함정 4건 | C |
| 복원 후 `normalize()` | C |
| 사이트별 추가 제외 선택자 | C |
| 3일 일정 + 버리는 순서 | C |
| `ruby` 플래그 · 다의 표제어 회수 | C |

## 부록 C. v2 로드맵 (B안 보관)

발표에서 "다음 단계는?"을 물으면 쓸 재료다. **이번에는 만들지 않는다.**

1. **방향 반전 — 일본어 원문 읽기 지원.** 아는 단어 비율에 따라 문장 단위로 원문/번역을 섞는
   3단 척도. 한국어와 일본어는 어순과 조사 구조가 거의 같아 「彼は剣を **뽑았다**」가 실제로
   읽힌다 — 영어에서는 성립하지 않는 설계다
2. **어휘 체크 목록.** 저장하는 상태는 목록 하나뿐이고 화면은 매번 재계산.
   `chrome.storage.sync` + JSON 내보내기/가져오기
3. **뉘앙스 복원.** 번역하면 죽는 표현(경어체↔반말, 1인칭 僕/俺/私)에 밑줄.
   설명은 **생성되지 않고 조회된다** — AI를 런타임에서 빌드 타임으로 옮기면 환각·원가·프라이버시가
   동시에 사라진다. 대신 사전이 늙는다
4. **색 규칙**: 원문으로 보이는 단어에 색이 있으면 "번역하면 죽는 것", 색이 없으면 "네가 아는 것"
5. **Chrome 내장 Translator API** (온디바이스, 서버 0). 단, **일→한 품질 실측이 선행 조건**
6. 거짓짝 블랙리스트, 형태소 분석기(MeCab/Sudachi), 빈도 코퍼스
7. 한 글자 표면형 화이트리스트 복원 (C안의 제외 사유 55종이 참고자료로 남아 있다)
